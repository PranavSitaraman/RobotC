// change the line numbers to make all sentences show up on screen at the same time.
// be inquisitive. Make a few changes and check it out
task main()
{
  displayTextLine(0, "Hello!");
  displayTextLine(1, "My name is Blah!");
  displayTextLine(2, "At Storming Robots" );
  delay(10000);
}
