// This one you must need the controller to play the sound
// be inquisitive. Change the sound and check it out
task main()
{
    playSound(soundFastUpwardTones);
    playSound(soundDownwardTones);
    playSound(soundLowBuzz);
    delay(10000);
}
